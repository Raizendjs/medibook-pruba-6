---
// El dashboard de usuario se unificó con /perfil/ (perfil + stats + accesos
// rápidos en un solo lugar). Esta ruta se mantiene solo por compatibilidad
// con links viejos.
return Astro.redirect("/perfil/");
---
